# github_action_dotnet
Docker commands:

docker build . => build the image
docker image tag {imageid} {registryname:tag}  => tag the image with hub registry name and tag 
docker push {registryname:tag}  => push the image to hub
